[project]
name = "iam-auditor"
version = "0.1.0"
description = "Audits IAM users for missing MFA and overly permissive policies"
requires-python = ">=3.11"
dependencies = [
    "audit-common",
    "boto3>=1.34",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"