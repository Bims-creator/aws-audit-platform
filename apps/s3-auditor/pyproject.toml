[project]
name = "s3-auditor"
version = "0.1.0"
description = "Auditor for s3-auditor"
requires-python = ">=3.11"
dependencies = [
    "audit-common",
    "boto3>=1.34",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
