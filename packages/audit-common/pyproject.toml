[project]
name = "audit-common"
version = "0.1.0"
description = "Shared Finding model and AWS session helper for aws-audit-platform"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.34",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"