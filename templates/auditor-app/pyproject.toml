[project]
name = "__APP_NAME__"
version = "0.1.0"
description = "Auditor for __APP_NAME__"
requires-python = ">=3.11"
dependencies = [
    "audit-common",
    "boto3>=1.34",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
